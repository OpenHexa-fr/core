[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "openhexa-core"
version = "0.1.0"
description = "Shared Elasticsearch layer for OpenHexa backends"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    # <9.0 : le client v9 envoie un header Accept incompatible avec un serveur 8.x
    # (media_type_header_exception). Rester sur la ligne 8.x tant que le cluster l'est.
    "elasticsearch[async]>=8.0,<9.0",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "structlog>=23.0",
    "polars>=0.20",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.5",
    "mypy>=1.10",
]

[tool.hatch.build.targets.wheel]
packages = ["openhexa_core"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC"]

[tool.mypy]
python_version = "3.11"
strict = true
plugins = ["pydantic.mypy"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
